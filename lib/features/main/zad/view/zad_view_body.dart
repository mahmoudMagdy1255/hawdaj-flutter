import 'package:flutter/material.dart';

class ZadViewBody extends StatelessWidget {
  const ZadViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container();
  }
}
