import 'package:flutter/material.dart';
import 'package:hawdaj/features/main/zad/view/zad_view_body.dart';

class ZadView extends StatelessWidget {
  const ZadView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ZadViewBody(),
    );
  }
}
